---
interface Props {
  de: string;
  en: string;
  as?: string;
  class?: string;
}
const { de, en, as = 'span', class: className } = Astro.props as Props;
const Tag = as as any;
---

<Tag class={className} data-i18n data-en={en} set:text={de} />
